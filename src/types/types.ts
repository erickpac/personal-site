export type Colors = {
  background: string;
  text: string;
  secondaryText: string;
  accent: string;
  accentHover: string;
  subtle: string;
  subtleHover: string;
};

export type Person = {
  name: string;
  role: string;
  company: string;
};

export type Job = {
  title: string;
  company: string;
  period: string;
  description: string;
};
