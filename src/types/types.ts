export type Colors = {
  background: string;
  text: string;
  secondaryText: string;
  accent: string;
  accentHover: string;
  subtle: string;
  subtleHover: string;
};

export type Coworker = {
  name: string;
  profilePicture: string;
  role: string;
  company: string;
  linkedin: string;
};

export type Job = {
  title: string;
  company: string;
  period: string;
  description: string;
};

export type Technology = {
  name: string;
  icon: string;
};
